import { UniqueEmailDirective } from '../directives/unique-email.directive';

describe('UniqueEmailDirective', () => {
  it('should create an instance', () => {
    //const directive = new UniqueEmailDirective();
    //expect(directive).toBeTruthy();
  });
});
